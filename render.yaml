# yaml-language-server: $schema=https://render.com/schema/render.yaml.json
previews:
  generation: off

databases:
  - name: watchroom-db
    region: frankfurt
    # Temporary closed-test profile. Upgrade before the database expires.
    plan: free
    postgresMajorVersion: "17"
    databaseName: watchroom
    user: watchroom
    ipAllowList: []

services:
  - type: web
    name: watchroom-api
    runtime: docker
    region: frankfurt
    plan: free
    numInstances: 1
    autoDeployTrigger: checksPass
    dockerContext: .
    dockerfilePath: ./apps/api/Dockerfile
    healthCheckPath: /health/ready
    maxShutdownDelaySeconds: 30
    # preDeployCommand is not available on Render Free. With one API instance,
    # migrate deploy is safe and idempotent as part of the startup command.
    dockerCommand: sh -c 'node node_modules/prisma/build/index.js migrate deploy && node apps/api/dist/index.js'
    buildFilter:
      paths:
        - apps/api/**
        - packages/shared/**
        - prisma/**
        - prisma.config.ts
        - package.json
        - pnpm-lock.yaml
        - pnpm-workspace.yaml
        - tsconfig.base.json
    envVars:
      - key: NODE_ENV
        value: production
      - key: API_HOST
        value: 0.0.0.0
      - key: API_PORT
        value: "10000"
      - key: LOG_LEVEL
        value: info
      - key: MOCK_TELEGRAM_AUTH
        value: "false"
      - key: SHUTDOWN_TIMEOUT_MS
        value: "25000"
      - key: DATABASE_URL
        fromDatabase:
          name: watchroom-db
          property: connectionString
      - key: PUBLIC_APP_URL
        sync: false
      - key: WEB_ORIGIN
        sync: false
      - key: API_URL
        sync: false
      - key: WS_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_WEBHOOK_SECRET
        sync: false
      - key: TELEGRAM_BOT_USERNAME
        sync: false
      - key: TELEGRAM_APP_SHORT_NAME
        sync: false
      - key: METRICS_BEARER_TOKEN
        generateValue: true
      - key: OPERATIONS_BEARER_TOKEN
        generateValue: true
      - key: YOUTUBE_API_KEY
        sync: false
      - key: TWITCH_CLIENT_ID
        sync: false
      - key: TWITCH_CLIENT_SECRET
        sync: false
